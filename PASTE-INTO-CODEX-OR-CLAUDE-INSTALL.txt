Use the local Starter Pack in this extracted folder or current workspace and install it now.

Do the work yourself. Do not ask me to use a terminal.

Steps:
1. Find `starter-pack\\INSTALL.ps1` in the current workspace or extracted folder.
2. Run it with PowerShell using the default build-ready path for first websites and apps.
3. Then run `starter-pack\\VERIFY.ps1 -Profile websites`.
4. If verification passes, tell me only:
- whether installation passed
- whether verification passed
- backup path
- anything optional that was skipped

Rules:
- Fix safe local problems yourself if you can.
- Ask me only if an irreversible or account-level choice blocks you.
- Do not explain the whole plan first. Just do the install and then report the result.
